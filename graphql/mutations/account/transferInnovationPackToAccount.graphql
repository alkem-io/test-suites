mutation TransferInnovationPackToAccount(
  $transferData: TransferAccountInnovationPackInput!
) {
  transferInnovationPackToAccount(transferData: $transferData) {
    id
    nameID
    listedInStore
    provider {
      id
      nameID
    }
    searchVisibility
  }
}
