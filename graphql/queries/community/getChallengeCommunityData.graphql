query getChallengeCommunity($spaceId: UUID_NAMEID!, $challengeId: UUID_NAMEID!) {
   space(ID: $spaceId) {
    id
    challenge(ID: $challengeId) {
      community {
        id
        ...MembersAndLeadsData

      }
    }
  }
}
