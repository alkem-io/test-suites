query GetOpportunityAvailableMembers(
  $spaceId: UUID_NAMEID!
  $opportunityId: UUID_NAMEID!
) {
  space(ID: $spaceId) {
    id
    # TODO: use lookup
    # opportunity(ID: $opportunityId) {
    #   community {
    #     id
    #     ...MembersAndLeadsData
    #   }
    # }
  }
}
