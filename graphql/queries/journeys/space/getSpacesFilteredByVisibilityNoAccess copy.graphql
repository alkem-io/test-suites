query GetSpacesFilteredByVisibilityWithoutAccess(
  $spaceIDs: [UUID!]
  $spaceFilter: SpaceFilterInput
) {
  spaces(IDs: $spaceIDs, filter: $spaceFilter) {
    nameID
    visibility
    authorization {
      ...AuthorizationSpaceData
    }
  }
}
