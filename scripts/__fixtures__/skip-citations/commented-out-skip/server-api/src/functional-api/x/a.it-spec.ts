describe('a suite', () => {
  // test.skip('an old test that was commented out entirely', () => {
  //   expect(1).toBe(1);
  // });

  test('a live, passing test', () => {
    expect(1).toBe(1);
  });
});
