query GetSpaceCommunication($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      community {
        communication {
          updates {
            messages {
              id
              message
              sender {
                id
              }
            }
          }
        }
      }
    }
  }
}
