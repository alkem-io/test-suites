query VirtualContributorKnowledgePrivileges($id: UUID!) {
  virtualContributor(ID: $id) {
    id
    knowledgeBase {
      id
      authorization {
        id
        myPrivileges
        __typename
      }
      __typename
    }
    __typename
  }
}
