query getMyUserInfo {
  me {
    user {
      id
      nameID
      email
      profile {
        id
        displayName
      }
      actor {
        id
      }
      account {
        id
      }
    }
  }
  platform {
    roleSet {
      myRoles
    }
  }
}
