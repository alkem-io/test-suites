query getMyUserInfo {
  me {
    user {
      id
      nameID
      email
      profile {
        id
        displayName
      }
      agent {
        id
      }
      account {
        id
      }
    }
  }
  platform {
    roleSet {
      myRoles
    }
  }
}
