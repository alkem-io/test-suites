# US7-AS4: proves that removing an associate deletes their organization
# in-app notification rows (062-organization-user-associates).
query MeInAppNotifications($types: [NotificationEvent!]) {
  me {
    notifications(filter: { types: $types }) {
      total
      inAppNotifications {
        id
        type
      }
    }
  }
}
