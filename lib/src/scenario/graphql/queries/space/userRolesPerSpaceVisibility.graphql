query GetUserRoles($rolesData: RolesActorInput!) {
  rolesUser(rolesData: $rolesData) {
    spaces {
      nameID
      id
      roles
      subspaces {
        nameID
        id
        roles
      }
      visibility
    }
    organizations {
      nameID
      id
      roles
    }
  }
}
