query GetInnovationFlowStatesWithIds($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      collaboration {
        innovationFlow {
          id
          states {
            id
            displayName
          }
        }
      }
    }
  }
}
