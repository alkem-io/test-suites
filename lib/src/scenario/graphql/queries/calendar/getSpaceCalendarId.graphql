query GetSpaceCalendarId($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      collaboration {
        timeline {
          calendar {
            id
          }
        }
      }
    }
  }
}
