query userEmailChangeAuditEntries($userID: UUID!) {
  platformAdmin {
    userEmailChangeAuditEntries(userID: $userID) {
      total
      auditEntries {
        id
        outcome
      }
    }
  }
}
