query PlatformRolesSpaceApplicationsProbe($spaceId: UUID!) {
  lookup {
    space(ID: $spaceId) {
      id
      community {
        roleSet {
          applications {
            id
          }
        }
      }
    }
  }
}
