query platformRoleSetOrganizationsInRoles($roles: [RoleName!]!) {
  platform {
    roleSet {
      organizationsInRoles(roles: $roles) {
        role
        organizations {
          id
        }
      }
    }
  }
}
