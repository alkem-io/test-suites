mutation UpdateCalloutSpacesSettings($calloutData: UpdateCalloutEntityInput!) {
  updateCallout(calloutData: $calloutData) {
    id
    settings {
      framing {
        commentsEnabled
        spaces {
          cardVariant
        }
        selection {
          mode
          selectedIds
        }
      }
    }
  }
}
