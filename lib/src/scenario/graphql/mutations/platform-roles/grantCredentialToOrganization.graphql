mutation grantCredentialToOrganization($grantCredentialData: GrantOrganizationAuthorizationCredentialInput!) {
  grantCredentialToOrganization(grantCredentialData: $grantCredentialData) {
    id
  }
}
