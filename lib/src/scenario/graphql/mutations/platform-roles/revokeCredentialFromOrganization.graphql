mutation revokeCredentialFromOrganization($revokeCredentialData: RevokeOrganizationAuthorizationCredentialInput!) {
  revokeCredentialFromOrganization(revokeCredentialData: $revokeCredentialData) {
    id
  }
}
