mutation TransferSpaceToAccount($transferData: TransferAccountSpaceInput!) {
  transferSpaceToAccount(transferData: $transferData) {
    subscriptions{
      expires
      name
    }
    account {
      id
    }
  }
}
