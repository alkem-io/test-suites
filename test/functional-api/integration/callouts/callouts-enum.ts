export enum CalloutState {
  ARCHIVED = 'ARCHIVED',
  CLOSED = 'CLOSED',
  OPEN = 'OPEN',
}

export enum CalloutType {
  CANVAS = 'CANVAS',
  CARD = 'CARD',
  DISCUSSION = 'DISCUSSION',
}

export enum CalloutVisibility {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
}
