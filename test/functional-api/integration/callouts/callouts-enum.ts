export enum CalloutState {
  ARCHIVED = 'ARCHIVED',
  CLOSED = 'CLOSED',
  OPEN = 'OPEN',
}

export enum CalloutType {
  WHITEBOARD = 'WHITEBOARD',
  POST = 'POST',
  COMMENTS = 'COMMENTS',
}

export enum CalloutVisibility {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
}
