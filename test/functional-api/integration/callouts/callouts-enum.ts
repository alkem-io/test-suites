export enum CalloutState {
  ARCHIVED = 'ARCHIVED',
  CLOSED = 'CLOSED',
  OPEN = 'OPEN',
}

export enum CalloutType {
  CANVAS = 'CANVAS',
  CARD = 'CARD',
  COMMENTS = 'COMMENTS',
}

export enum CalloutVisibility {
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
}
