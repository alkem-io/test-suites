# Bot-owned test-plan run summaries (on gh-pages only, but flagged here so
# that any incidental inclusion is treated as generated content, not hand-
# authored — spec 004, T058).
gh-pages-root/test-plans/runs/**/*.json linguist-generated=true
test-plans/dist/** linguist-generated=true
